[package]
name = "xgen-core"
version = "0.1.0"
edition = "2021"
description = "Platform-neutral trusted core contracts for XGEN Side"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

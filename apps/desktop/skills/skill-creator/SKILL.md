---
name: skill-creator
description: Create or update validated XGEN Side skill packages with concise instructions, activation metadata, permissions, and runtime bindings.
---

# Skill Creator

Create a lowercase hyphenated skill folder containing `SKILL.md`, `manifest.json`, and optional references only when needed. Define narrow activation signals and least-privilege permissions, then validate loading and routing with tests.
